"""Application domains.

The project is moving from a flat module layout to explicit business domains:
core, crm, and inspect. Existing flat modules remain as compatibility shims
while the codebase is migrated gradually.
"""

